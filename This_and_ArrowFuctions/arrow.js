//syntax of arrow function

const any = () => {
    let name ="siddhant"
    console.log(name);
}
any();
//------------------------------------------

//If the function body has a single expression, you can omit the return statement and the curly braces {}
//Arrow functions automatically return the result of the expression.
const add = (a, b) => a + b;